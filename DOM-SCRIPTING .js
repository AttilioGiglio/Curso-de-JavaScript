/* El DOM (Document object Model). El document object es el padre del HTML tienes elementos (etiquetas) y los nodos que son los que conectan los elementos y forman el root(file.html).DOM transforma todos los elementos HTML en un conjunto de elementos llamados nodos, que están interconectados y que representan los contenidos de las páginas web y las relaciones entre ellos. Por su aspecto, la unión de todos los nodos se llama "árbol de nodos". JS TE PERMITE ACCEDER A TODOS LOS ELEMENTOS DEL DOM*/

let element;

element = document;
element = document.all;
element = document.body;
element = document.domain;
element = document.p.id;

console.log(element)